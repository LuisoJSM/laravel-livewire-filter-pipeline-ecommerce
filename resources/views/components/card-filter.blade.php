<div class="card filter-card shadow-sm border-0 rounded-3">
    <div class="card-header bg-white border-0 py-3">
        <h6 class="mb-0 text-uppercase small text-muted fw-semibold">{{ $title }}</h6>
    </div>

    <div class="card-body p-0">
        {{ $slot }}
    </div>
</div>
